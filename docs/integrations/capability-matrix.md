# Multi-Provider Capability Matrix

P0 gate artifact. This matrix records, per provider, whether a behaviour signal is
actually derivable from observed telemetry before any canonical type or dashboard is
built on it. No cell may be raised above `unknown` without version-pinned fixture evidence
committed under `fixtures/<provider>/` and referenced in the Evidence column.

States: `supported | partial | unsupported | unknown | version-dependent`.

Provider-specific per-tool findings:
- Codex: [codex-capability-matrix.md](codex-capability-matrix.md), [codex-phase-0-findings.md](codex-phase-0-findings.md)
- Claude Code: [claude-code-capability-probe.md](claude-code-capability-probe.md)
- Cursor Agent: [cursor-agent-capability-probe.md](cursor-agent-capability-probe.md)
  (local-dev stream-json). Org/SaaS path: [cursor-enterprise-otel.md](cursor-enterprise-otel.md)
  (Cursor Enterprise server-side OTLP protobuf; epic #128).

## Observed telemetry sources per provider

| Provider | Version observed | Sources |
|---|---|---|
| Claude Code | 2.1.251 / 2.1.263 skill capture on 2026-09-06; 2.1.268 metrics and trace-span capture on 2026-09-11; 2.1.269 session JSONL transcript capture on 2026-09-12; 2.1.270 tool-decision capture on 2026-09-13; 2.1.270 prompt/response + raw API body content capture on 2026-09-13 | Local CLI version probe; OTLP HTTP/JSON log events from synthetic runs; OTLP HTTP/JSON metrics from a synthetic run (`claude_code.token.usage`), plus a docs-shaped synthetic metrics fixture pairing `claude_code.token.usage` and `claude_code.cost.usage` with per-datapoint attribution dims (`fixtures/claude/observed-sanitised/claude-code-2.1.268-cost-attribution-metrics.json`, #97), plus a docs-shaped synthetic metrics fixture carrying the three code-output counters (`claude_code.lines_of_code.count`, `claude_code.commit.count`, `claude_code.pull_request.count`; `fixtures/claude/observed-sanitised/claude-code-2.1.268-code-output-metrics.json`, #98), plus a docs-shaped synthetic metrics fixture carrying the three engagement metrics (`claude_code.code_edit_tool.decision`, `claude_code.session.count`, `claude_code.active_time.total`; `fixtures/claude/observed-sanitised/claude-code-2.1.268-engagement-metrics.json`, #99); OTLP HTTP/JSON trace spans from a synthetic run under the enhanced-telemetry beta (`claude_code.interaction` → `claude_code.llm_request`); session JSONL transcript import via `POST /v1/claude/transcript` (`NormalizeTranscript`); `tool_decision` permission-decision log events from a synthetic run (`OTEL_LOG_TOOL_DETAILS=1`, one config-allow and two hook-denials); content-bearing `user_prompt`/`assistant_response`/`api_request_body`/`api_response_body` log events from a synthetic run with content logging enabled (`OTEL_LOG_USER_PROMPTS=1`, `OTEL_LOG_ASSISTANT_RESPONSES=1`, `OTEL_LOG_RAW_API_BODIES=1`, `OTEL_LOG_TOOL_DETAILS=1`; synthetic prompts only); `fixtures/claude/observed-sanitised/claude-code-2.1.251-capability-probe.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.251-otlp-events.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.263-skill-activated.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.268-token-usage-metrics.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.268-trace-spans-otlp.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.269-session-transcript.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-tool-decision.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-tool-decision-otlp.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-user-prompt.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-user-prompt-otlp.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-assistant-response.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-assistant-response-otlp.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-api-bodies.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.270-api-bodies-otlp.json`, `fixtures/claude/observed-sanitised/claude-code-2.1.268-engagement-metrics.json` |
| Cursor Agent | 2026.05.16-0338208 probed on 2026-08-31; Enterprise OTEL wire `cursor.telemetry` 0.1.0 synthetic fixtures on 2026-09-12 (#130) | Local CLI version probe; authenticated print JSON result from synthetic run; local OTLP HTTP/JSON export not observed; Enterprise path: synthetic OTLP metrics (`cursor.token.usage`) and logs (`cursor.api.request`) normalised by `cursor.NormalizeMetrics`/`NormalizeLogs`; `fixtures/cursor/observed-sanitised/cursor-agent-2026.05.16-0338208-capability-probe.json`, `fixtures/cursor/observed-sanitised/cursor-agent-2026.05.16-0338208-print-result.json`, `fixtures/cursor/observed-sanitised/cursor-otel-0.1.0-token-usage-metrics.json`, `fixtures/cursor/observed-sanitised/cursor-otel-0.1.0-api-request-logs.json` |
| Codex | CLI 0.145.0 logs fixture; current CLI 0.153.4 surface captured on 2026-09-10 | OTLP JSON logs and metrics to loopback receiver; `fixtures/codex/observed-sanitised/codex-0.145.0-logs.json`; current signal inventory under `fixtures/codex/observed-sanitised/current-0.153.4-surface/` |

## Capability matrix

| Capability | Codex | Claude Code | Cursor | Notes |
|---|---|---|---|---|
| Model identity | supported | supported | partial | Codex: `model` attribute observed in `fixtures/codex/observed-sanitised/codex-0.145.0-logs.json`, now extracted into `canonical.ModelInteraction.model` by `codex.ExtractLogModelInteractions` (absent → `"unknown"`, provenance downgraded). Claude Code: `model` observed in `fixtures/claude/observed-sanitised/claude-code-2.1.251-otlp-events.json`, now extracted into `canonical.ModelInteraction.model` by `claude.ExtractModelInteractions` (absent → `"unknown"`, provenance downgraded); the session JSONL transcript's assistant-record `message.model` is also surfaced into `assistant_message` event `attributes.model` by `claude.NormalizeTranscript` (`fixtures/claude/observed-sanitised/claude-code-2.1.269-session-transcript.json`, tool 2.1.269, #91). Cursor Agent: print JSON alone lacks model; stream-json system init includes `model` in `fixtures/cursor/observed-sanitised/cursor-agent-2026.09.02-c22c1a3-stream-result-with-model.json` (tool 2026.09.02-c22c1a3) and is extracted by `cursor.ExtractModelInteractions`. Cursor Enterprise OTEL: optional `cursor.model.name` on `cursor.token.usage` / `cursor.api.request` is promoted when present (`fixtures/cursor/observed-sanitised/cursor-otel-0.1.0-token-usage-metrics.json`, `…-api-request-logs.json`, #130). |
| Token: input/output | supported | supported | supported | Codex: input/output token attributes observed in `fixtures/codex/observed-sanitised/codex-0.145.0-logs.json`; current `codex.turn.token_usage` also exposes `token_type=input` and `token_type=output` in `fixtures/codex/observed-sanitised/current-0.153.4-surface/codex-0.153.4-metric-codex-turn-token-usage.json`. Claude Code: input/output token counts observed in `fixtures/claude/observed-sanitised/claude-code-2.1.251-otlp-events.json`, now extracted into `ModelInteraction.input_tokens`/`output_tokens` by `claude.ExtractModelInteractions` (nil when absent, never a fabricated zero); additionally the live `claude_code.token.usage` OTLP metric (`type=input`/`output`) is now ingested via `/v1/metrics` by `claude.NormalizeMetrics` into `input_token_count`/`output_token_count` (`fixtures/claude/observed-sanitised/claude-code-2.1.268-token-usage-metrics.json`, tool 2.1.268, #89); per-datapoint attribution dims (`skill.name`, `mcp_server.name`, `mcp_tool.name`, `agent.name`, `plugin.name`, `marketplace.name`, `speed`, `effort`) now survive into `provider_extensions.metric_attributes` so token spend is attributable to the skill/MCP tool/sub-agent/plugin that burned it (`fixtures/claude/observed-sanitised/claude-code-2.1.268-cost-attribution-metrics.json` → `fixtures/claude/expected/claude-code-2.1.268-cost-attribution-metrics.events.json`, #97 M10); the code-output metrics (`lines_of_code`/`commit`/`pull_request`) are mapped by M11 (#98, see the Code output row) and the engagement metrics (`code_edit_tool.decision`/`session.count`/`active_time.total`) by M12 (#99, see the Engagement row), completing the documented metric surface; the session JSONL transcript's `message.usage.input_tokens`/`output_tokens` are also mapped into `input_token_count`/`output_token_count` by `claude.NormalizeTranscript` (tool 2.1.269, #91). Cursor Agent: input/output token counts observed in `fixtures/cursor/observed-sanitised/cursor-agent-2026.05.16-0338208-print-result.json` and extracted by `cursor.ExtractModelInteractions`. Cursor Enterprise OTEL: `cursor.token.usage` (`cursor.token.type=input`/`output`) via `cursor.NormalizeMetrics` and `cursor.api.request` token attributes via `cursor.NormalizeLogs` (`fixtures/cursor/observed-sanitised/cursor-otel-0.1.0-token-usage-metrics.json`, `…-api-request-logs.json`, #130); `cursor.tool.calls` / `cursor.cost.usage` and other log families remain route-tolerated/unmapped. |
| Token: cached | supported | supported | supported | Codex: current `codex.turn.token_usage` exposes `token_type=cached_input` and `token_type=cache_write_input` in `fixtures/codex/observed-sanitised/current-0.153.4-surface/codex-0.153.4-metric-codex-turn-token-usage.json`. Claude Code: cache-read and cache-creation token counts observed in `fixtures/claude/observed-sanitised/claude-code-2.1.251-otlp-events.json`; `claude.ExtractModelInteractions` maps `cache_read_tokens` to `ModelInteraction.cached_input_tokens` and preserves `cache_creation_tokens` under `provider_extensions` (no canonical cache-creation field yet, so it is not conflated); the live `claude_code.token.usage` metric (`type=cacheRead`/`cacheCreation`) is now ingested via `/v1/metrics` by `claude.NormalizeMetrics` into `cached_input_token_count`/`cache_write_input_token_count`; the session JSONL transcript's `message.usage.cache_read_input_tokens`/`cache_creation_input_tokens` map to the same keys via `claude.NormalizeTranscript`, and the ephemeral cache-window counts (`cache_creation.ephemeral_*`) are preserved under `provider_extensions.cache_usage_extra` (tool 2.1.269, #91). Cursor Agent: cache-read and cache-write token counts observed in `fixtures/cursor/observed-sanitised/cursor-agent-2026.05.16-0338208-print-result.json`; `cursor.ExtractModelInteractions` maps cache-read into `cached_input_tokens` and preserves cache-write under `provider_extensions`. Cursor Enterprise OTEL: `cursor.token.type=cache_read`/`cache_creation` and `cursor.api.request.cache_*_tokens` map to `cached_input_token_count`/`cache_write_input_token_count` (#130). |
| Token: reasoning | supported | supported | unknown | Codex: current `codex.turn.token_usage` exposes `token_type=reasoning_output` in `fixtures/codex/observed-sanitised/current-0.153.4-surface/codex-0.153.4-metric-codex-turn-token-usage.json`. Claude Code: the session JSONL transcript's `message.usage.output_tokens_details.thinking_tokens` is mapped into `reasoning_token_count` by `claude.NormalizeTranscript` (`fixtures/claude/observed-sanitised/claude-code-2.1.269-session-transcript.json` → `fixtures/claude/expected/claude-code-2.1.269-session-transcript.events.json`, tool 2.1.269, #91). No reviewed Cursor fixture proves reasoning-token data. |
| Cost (provider-reported) | unknown | partial | unknown | Claude Code: the `claude_code.cost.usage` OTLP metric is ingested via `/v1/metrics` by `claude.NormalizeMetrics` into a `canonical.Event` (`event_type = claude_code.cost.usage`) carrying the provider-reported USD amount under `attributes.provider_cost`, with the same per-datapoint attribution dims (`skill.name`/`mcp_server.name`/`mcp_tool.name`/`agent.name`/`plugin.name`/`marketplace.name`) in `provider_extensions.metric_attributes` (`fixtures/claude/observed-sanitised/claude-code-2.1.268-cost-attribution-metrics.json` → `fixtures/claude/expected/claude-code-2.1.268-cost-attribution-metrics.events.json`, #97 M10). Cost is **secondary/descriptive** (`PRODUCT_MAP.md` §0): the amount is preserved as evidence but never priced by `cost.Calculate` (which early-returns `claude_code.cost.usage` to avoid double-counting request-level pricing) — hence `partial`, not `supported`. The live metric carrying attribution dims was not reachable in the capture environment, so the fixture is shaped from the documented `cost.usage` surface and marked `fixture_origin: synthetic`. Codex/Cursor: `codex.*`/`cursor.cost.usage` cost metrics remain route-tolerated/unmapped. |
| Tool calls (generic) | partial | partial | unknown | Codex `codex.tool_result` logs map to event-level tool-call signals and `canonical.Operation` records using `fixtures/codex/observed-sanitised/codex-0.153.4-outcome-contracts-otlp.json` and `fixtures/codex/expected/codex-0.153.4-tool-result.operations.json`; Codex `codex.sandbox_outcome` logs map to command-execution operation signals using `fixtures/codex/observed-sanitised/codex-0.153.4-sandbox-outcome-otlp.json` and `fixtures/codex/expected/codex-0.153.4-sandbox-outcome.operations.json`. Metric-only tool counters remain follow-up surfaces. Claude Code `tool_result` events (`tool_name`, `success`, `duration_ms`, `error_type`, `decision_type`/`decision_source`, size counters) map to `canonical.Operation` records via `claude.ExtractOperations` using `fixtures/claude/observed-sanitised/claude-code-2.1.263-tool-result.json` and `fixtures/claude/expected/claude-code-2.1.263-tool-result.operations.json` (tool 2.1.263, #92); the raw OTLP wire shape is `fixtures/claude/observed-sanitised/claude-code-2.1.263-tool-result-otlp.json`. `success` (string `"true"`/`"false"`) derives the operation outcome; category is classified from `tool_name` (Bash → shell command, Read/Glob/Grep → filesystem read, Write/Edit → filesystem write, `mcp__…`/`mcp_server_scope` → MCP call, else unknown); tool input parameters and result bodies stay out of scope (gated content, JSONL issues). Cursor Agent result does not include tool calls. |
| Approval / permission decisions | supported | supported | unknown | Codex `codex.tool_decision` logs map to event-level approval signals using `fixtures/codex/observed-sanitised/codex-0.153.4-tool-decision-otlp.json` and `fixtures/codex/expected/codex-0.153.4-tool-decision.events.json`. Claude Code `tool_decision` events (`decision`, `source`, `tool_name`, `tool_source`, `tool_use_id`) map to a `canonical.Event` approval signal via `claude.attachToolDecision` using `fixtures/claude/observed-sanitised/claude-code-2.1.270-tool-decision.json` and `fixtures/claude/expected/claude-code-2.1.270-tool-decision.events.json` (tool 2.1.270, #93); the raw OTLP wire shape is `fixtures/claude/observed-sanitised/claude-code-2.1.270-tool-decision-otlp.json`. The wire `accept`/`reject` normalises to the cross-provider `approved`/`denied` vocabulary (`approval_decision`; missing → `unknown`, never inferred), with the untouched value kept under `provider_extensions.tool_decision.raw_decision`; `source` becomes `approval_reason_class`. Reject decisions appear only here — `tool_result.decision_type` is always `accept`. The gated `tool_parameters` attribute (full command / MCP server+tool names, present under `OTEL_LOG_TOOL_DETAILS=1`) is dropped at the wire boundary by `NormalizeLogs` and never surfaced. Cursor approval signals remain unevidenced in committed fixtures. |
| MCP calls | unknown | partial | unknown | Claude Code emits MCP server connection events in `fixtures/claude/observed-sanitised/claude-code-2.1.251-otlp-events.json`, normalised by `claude.NormalizeEvents` into a `canonical.Event` (a connection lifecycle event, deliberately not a fabricated `Operation`/tool call). An executed MCP tool call is also observed: an `mcp__…` `tool_result` in `fixtures/claude/observed-sanitised/claude-code-2.1.263-tool-result.json` maps via `claude.ExtractOperations` to a `canonical.Operation` with category `MCP call` (`fixtures/claude/expected/claude-code-2.1.263-tool-result.operations.json`). MCP-call-specific correlation (matching an invocation back to its server connection/inventory) is the remaining gap, owned by #104. Codex and Cursor fixtures do not prove MCP calls. |
| Skill invocations | version-dependent | supported | unknown | Mark `explicit | inferred | unavailable` only after event fixture evidence. Claude Code: `skill_activated` with `skill.name` / `invocation_trigger` / `skill.source` in `fixtures/claude/observed-sanitised/claude-code-2.1.263-skill-activated.json` (tool 2.1.263); `claude.NormalizeEvents`/`NormalizeLogs` stamp `skill_detection=explicit`. Codex: `codex.skill.injected` with `skill` stamps explicit named skill usage; live trigger-style `bmad:brainstorm` on CLI 0.153.4 emitted `codex.skill.turn.duration_seconds` with no skill name, so `codex.NormalizeMetrics` stamps only inferred coverage. Cursor: no skill fixture. |
| File operations | unknown | unknown | unknown | Needed for risky-access governance; no reviewed event fixture proves file-operation telemetry. |
| Session-state & governance | unknown | supported | unknown | Claude Code: three lifecycle events normalised into governance contracts under `provider_extensions.governance` by `claude.attachGovernanceContext` (E9, #96) — `permission_mode_changed` (`from_mode`/`to_mode`/`trigger`, with `bypass_permissions` flagged on a transition into `bypassPermissions`), `auth` (`action`/`success`/`auth_method`, plus `error_category`/`status_code` on failure), and `plugin_loaded` (`plugin.scope`, `enabled_via`, pre-hashed `plugin_id_hash`, `has_hooks`/`has_mcp`/`host_owned_mcp`, skill/command/agent path counts, `safe_mode`). Fixtures `fixtures/claude/observed-sanitised/claude-code-2.1.273-session-governance.json` (+ `-otlp` wire pair) → golden `fixtures/claude/expected/claude-code-2.1.273-session-governance.events.json`. `plugin_loaded` is a genuine live capture (claude 2.1.273 with a scaffolded local plugin against a loopback OTLP sink, unauthenticated so no API call); `permission_mode_changed`/`auth` need interactive TUI/OAuth flows and are reproduced verbatim from the 2.1.273 binary's own event-emit code. These events are absent from the published monitoring docs but confirmed emitted by the shipped CLI. Codex/Cursor: no reviewed governance fixture. |
| Session boundaries | partial | partial | partial | Codex: `conversation.id` observed in `fixtures/codex/observed-sanitised/codex-0.145.0-logs.json`; local-only output uses the raw `codex:<conversation.id>` when present. Current 0.153.4 lifecycle evidence maps `codex.conversation_starts` to canonical `session.active` and retains safe startup/websocket governance metadata from `codex.startup_phase` / `codex.websocket_connect`; no reviewed session-end signal is proven. Codex metrics without a conversation ID remain observation-only and are excluded from the default session list. Claude Code: session identifier observed in `fixtures/claude/observed-sanitised/claude-code-2.1.251-otlp-events.json`; local-only output uses the raw `claude-code:<session_id>` and keeps request IDs raw. Trace-only fallbacks remain observations. Cursor Agent: session identifier observed in `fixtures/cursor/observed-sanitised/cursor-agent-2026.05.16-0338208-print-result.json`; local-only output uses the raw `cursor-agent:<session_id>` and keeps request IDs raw. Cursor Enterprise OTEL: `cursor.conversation.id` on `api.request` logs becomes `cursor:<conversation.id>`; metrics without conversation ids use a content-derived observation ID and are excluded from the default list. `cursor.team.id` / `cursor.user.id` are never promoted (account identifiers). Task boundaries remain `unknown` unless a reviewed provider signal is observed; no timing/model-based merges are inferred. |
| Trace/span correlation | unsupported | supported | unknown | Codex current capture observed no `/v1/traces` POSTs and log trace/span IDs remained empty; see `fixtures/codex/observed-sanitised/current-0.153.4-surface/codex-0.153.4-traces-endpoint.json`. Claude Code: the enhanced-telemetry beta exports a span tree (`claude_code.interaction` → `claude_code.llm_request`) over `POST /v1/traces`, now ingested by `claude.NormalizeTraces` into one canonical event per span with the raw `traceId`/`spanId`/`parentSpanId` preserved under `provider_extensions.span`/`correlation` (`fixtures/claude/observed-sanitised/claude-code-2.1.268-trace-spans-otlp.json` → `fixtures/claude/expected/claude-code-2.1.268-trace-spans.events.json`, tool 2.1.268, #90; resolves the `/v1/traces` 501 of #50). Per-span-type field mapping for the two per-prompt span types is done (#100, T13): `attributes.interaction` (`sequence`, `duration_ms`, `user_prompt_length`, `parent_source`, `queued_sends`) and `attributes.llm_request` (`model`, `gen_ai.*`, `context`, latency, token counts, `attempt`/`success`/`status_code`, `stop_reason`, `finish_reasons` decoded from the OTLP arrayValue, `response_has_tool_call`, and sub-agent `agent_id`/`parent_agent_id`/`workflow.*`), all present-only, with the interaction root's `task_boundary.confidence` raised to `observed`; the raw free-text `error` message is dropped, only the bounded `error_class` is kept. Tool/hook/sub-agent span-type mapping remains owned by T-phase #101–#103. |
| Task outcome | partial | partial | partial | Claude Code: `api_request` (success), `api_error` (failed), and `api_refusal` (refused) provider-completion contracts in `fixtures/claude/observed-sanitised/claude-code-2.1.263-api-request-outcome.json`, `…-api-error-outcome.json`, and `…-api-refusal-outcome.json` (+ `-otlp` wire pair, tool 2.1.263). A refusal (Messages API `stop_reason: "refusal"`) is a third outcome — neither success nor error, arriving as HTTP 200 — mapped by `claude.attachOutcomeContract` to `status: refused` with `server_fallback_hop`/`has_category`/`category`/`has_explanation`/`explanation` carried verbatim (golden `fixtures/claude/expected/claude-code-2.1.263-api-refusal-outcome.events.json`, #95). The refusal fixture is `fixture_origin: synthetic`: a live capture was attempted against a loopback OTLP sink, but eliciting a real `stop_reason: "refusal"` requires a frontier-safety prompt that was not authored, and a benign reasoning-extraction probe produced only a normal decline — so the shape is reproduced synthetically from the documented `claude_code.api_refusal` schema. Codex: `codex.api_request` success/failed + `attempt`, and `codex.tool_result` success/failed with `model` in `fixtures/codex/observed-sanitised/codex-0.153.4-outcome-contracts.json` (CLI 0.153.4; live unittest used `exec_command`). Cursor Agent: print/stream result `subtype`/`is_error` plus stream-json init `model` in `fixtures/cursor/observed-sanitised/cursor-agent-2026.09.02-c22c1a3-stream-result-with-model.json` — broader PR/revert/abandon contracts still unobserved. |
| Prompt/response content | unsupported | supported | unsupported | Codex fixture `fixtures/codex/observed-sanitised/codex-0.145.0-logs.json` has no retained log body; content capture remains out of scope by default. Claude Code: the four content-bearing `/v1/logs` events — `user_prompt`, `assistant_response`, `api_request_body`, `api_response_body` — are normalised by `claude.NormalizeEvents`/`NormalizeLogs` into `canonical.Event`s that echo the prompt/response text and raw API `body`/`body_ref` verbatim under `provider_extensions.event` (no typed content record; content rides the generic path like `api_request`'s numerics), captured raw with no ingest-time re-redaction (epic #87 / #94). Per-event content availability is precise: `prompt_content` on `user_prompt` and `api_request_body`, `response_content` (and `model`) on `assistant_response` and `api_response_body`; the length-only `user_prompt` proves `prompt_length` is reported with the prompt absent (never fabricated), and the default `assistant_response` proves the provider `"<REDACTED>"` sentinel is captured as received. The bare `prompt.id`/`message.uuid` correlation ids are dropped at the wire boundary (owned by #106). Fixtures `fixtures/claude/observed-sanitised/claude-code-2.1.270-user-prompt.json`, `…-assistant-response.json`, `…-api-bodies.json` (+ `-otlp` wire pairs) → goldens `fixtures/claude/expected/claude-code-2.1.270-user-prompt.events.json`, `…-assistant-response.events.json`, `…-api-bodies.events.json` (tool 2.1.270, #94). The shared fixture validator no longer prohibits the `prompt`/`response` field names, but its value-based secret/entropy scan still guards every string so no real credential can be committed. Cursor Agent fixture records only a synthetic result summary in `fixtures/cursor/observed-sanitised/cursor-agent-2026.05.16-0338208-print-result.json`. |
| Code output (lines / commits / PRs) | unknown | supported | unknown | Claude Code: the three code-output metrics — `claude_code.lines_of_code.count` (`type=added`/`removed`, `model`), `claude_code.commit.count`, `claude_code.pull_request.count` — are ingested via `/v1/metrics` by `claude.NormalizeMetrics` into one `canonical.Event` per datapoint. `lines_of_code.count` maps the `type` dimension to `attributes.lines_added_count`/`lines_removed_count` (mirroring how token `type` selects its count key) and promotes `model`; `commit.count`/`pull_request.count` carry `attributes.commit_count`/`pull_request_count` (standard attrs only, no `model`). The `lines_of_code` per-datapoint attribution dims (`skill.name`, `agent.name`, …) survive into `provider_extensions.metric_attributes`, so produced code is attributable to the skill/sub-agent that produced it — the outcome half of the spend-vs-output view (`fixtures/claude/observed-sanitised/claude-code-2.1.268-code-output-metrics.json` → `fixtures/claude/expected/claude-code-2.1.268-code-output-metrics.events.json`, tool 2.1.268, #98 M11). These are direct outcome counters, not attributed to individual files (per-file LOC is JSONL diffs, #105). The live metric carrying these dims was not reachable in the capture environment, so the fixture is shaped from the documented surface and marked `fixture_origin: synthetic`. Codex/Cursor: no reviewed code-output metric fixture. |
| Engagement (edit decisions / sessions / active time) | unknown | supported | unknown | Claude Code: the final three exported metrics — `claude_code.code_edit_tool.decision`, `claude_code.session.count`, `claude_code.active_time.total` — are ingested via `/v1/metrics` by `claude.NormalizeMetrics` into one `canonical.Event` per datapoint. Following the M11 split rule, a metric becomes distinct count keys only when its categorical names distinct *measures* (as `lines_of_code`'s `type=added/removed` does); when the categorical instead *partitions a single measure* it stays one key with the categorical surviving as a dimension. So `code_edit_tool.decision` → `attributes.edit_decision_count` with `decision` (accept/reject), `tool_name`, `source`, `language` surviving in `provider_extensions.metric_attributes` (edit-acceptance rate is a group-by-then-divide over `decision`); `session.count` → `attributes.session_count` with `start_type` (fresh/resume/…) surviving; `active_time.total` is a **duration** (unit `s`) → `attributes.active_time_seconds` via a non-negative float path (fractional seconds preserved, genuine `0` kept, negative → hard error), with its `type` dim *promoted* to `attributes.activity_type` (user/cli) rather than allow-listed, so the token/lines goldens that already carry `type` on the wire stay byte-identical. Identity attrs (`user.id`/`user.email`/`session.id`/`terminal.type`) are dropped by the metric-attribute allow-list. The issue body's draft `source` attribute for `session.count` is stale — the real wire key is `start_type`. Live metrics carrying these dims were not reachable in the capture environment, so the fixture is shaped from the documented surface and marked `fixture_origin: synthetic` (`fixtures/claude/observed-sanitised/claude-code-2.1.268-engagement-metrics.json` → `fixtures/claude/expected/claude-code-2.1.268-engagement-metrics.events.json`, tool 2.1.268, #99 M12). Codex/Cursor: no reviewed engagement metric fixture. |

## How to raise a cell above `unknown`

1. Capture a sanitised, version-pinned fixture for the provider.
2. Commit expected canonical output and warnings alongside it where normalisation is in scope.
3. Update the cell with state and a one-line evidence pointer to the fixture path and tool version.
4. Keep provider-specific shapes in `provider_extensions` until two independent providers produce semantically equivalent data.
5. Keep absent evidence as `unknown` or `unsupported`; do not infer capabilities from configuration, documentation, or provider marketing copy.
6. Never mark a cell `unavailable`/`unsupported` (or stamp event-level `skill_detection: unavailable`) to avoid capturing a harder telemetry surface; capture the skill-using run, session JSONL, or OTLP metrics, or schedule that capture explicitly.
